<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="NewBoy.Views.Profile">
	

	<ListView x:Name="ProfileListView" HorizontalOptions="FillAndExpand" HasUnevenRows="true">
		<ListView.ItemTemplate>
			<DataTemplate>
				<ViewCell>
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="6*"/>
							<RowDefinition Height="2*"/>
							<RowDefinition Height="2*"/>
						</Grid.RowDefinitions>
						<Label Text="{Binding location}" FontSize="25" FontFamily="Calibri" Grid.Row="0"/>
							<Label Text="{Binding usersname}" Grid.Row="1" FontSize="14" FontAttributes="Bold"/>
							<Label Text="{Binding description}" Grid.Row="2" FontSize="12"/>
					</Grid>
				</ViewCell>
			</DataTemplate>
		</ListView.ItemTemplate>
		<ListView.Footer><Label/></ListView.Footer>
	</ListView>

</ContentPage>